import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  card: {
    marginBottom: 16,
  },
  price: {
    fontWeight: 'bold',
    marginTop: 8,
  },
});

export default styles;
